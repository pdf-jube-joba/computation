# 現状の整理
- 各モデルごとの Web について
    - `.js` の被りが多い
    - `index.html` を書くのも無駄
- book について
    - `.md` に直接 `<script>` を書くのもめんどくさい
    - 具体例が `book/src/assets/` にあるのもよくない
    - `makefile` と `serve.sh` を使うと `/out` にいかないといけない
- インタラクティブにするよりも、アニメーションだけ表示して、ユーザーが触るところは共通化したほうが楽
- 各モデルの共通部分を作って Web もできる限り統一したい

# 案
- 計算モデルで共通の API を trait にする？
- Web 側の統一の仕方？
